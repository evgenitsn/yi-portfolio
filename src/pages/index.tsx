import { PageTitle } from '../components';
import { Layout } from '../layout';

const Home: React.FC = () => {
  return (
    <Layout>
      <PageTitle>Home</PageTitle>
    </Layout>
  );
};

export default Home;
